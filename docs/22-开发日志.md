# learnMath 开发日志（每完成一块 → 记录 → 提交 → 推送）

> 规则（目标约定）：**每完成一块任务就生成相应文档并提交到远程**。本日志为逐块记录，
> 状态表（21 号）看进度，本文件留"做了什么 / 证据 / 踩坑"的工程证据链。

---

## W1 · 后端地基与领域核心（7 模块 ✅ · 58 单测）

### W1-B00~B05 领域核心批（commit `f5dc2a1`）
- **产出**：双 Application + ApiResponse/ErrorCode(04§3 全枚举)/GlobalExceptionHandler + actuator 健康；domain 五大纯函数：`JudgeService`（02§5.4 归一化流水线）、`MasteryCalculator`（20§1.2 唯一公式）、`UnlockRuleEvaluator`（02§5.3+20§1.3 持久解锁）、`SrsPlanner`（10§5.5+20§1.3 复习规则）、`PointsRules`（02§5.7 定价）。
- **证据**：`mvn test` 46/46 绿（Judge 12 · Mastery 11 · Unlock 9 · Srs 8 · Points 6）。
- **坑**：① `PointsRules` 行尾注释吞 `) {` → 编译错；② 测试断言手误（earn 180→1020）。

### W1-B06 认证策略批（commit `2f93653`）
- **产出**：`TokenCodec`（**纯 JDK HS256**，双域独立密钥，域分类→验签→过期顺序）、`ChannelPolicy`（M2 验证码为主 + 未配渠道降级 3200 + 协议版本 2007）。
- **证据**：+12 单测 → 累计 **58/58 绿**；**W1 出口达成**。
- **坑**：① Boot 4 的 Jackson 3 包名迁移（`tools.jackson`）→ domain 不引 JSON 库，手写 payload/正则解析；② 跨域 token 先验签会 2001 抢跑 → 改为**域声明分类(2004)→验签(2001)→过期(2003)**。

---

## W2 · 内容域（进行中：B-07/B-09/B-10 ✅ → 累计 10/75）

### W2-A · B-07 内容域迁移 SQL（本轮）
- **产出**：`server/src/main/resources/db/migration/V1__content_domain.sql`（**内容域 23 表**：T02-T14 树/课程/题库 + T50-T54 解法链四要素/翻译 + T55-T58 公式七件套 + T59 article；含 §8.2 subject 二型、§10 is_preview、§11.2 formula_id、BR-06 的 published_snapshot 列）+ `scripts/check-migrations.mjs` 结构校验。
- **证据**：`node scripts/check-migrations.mjs` → **23/23 表通过**（无 DROP/TRUNCATE、主键/审计字段齐、无重复）→ `docs/review/migration-check.json`。
- **坑**：校验正则初版只认 `(id)` 括号式主键，与 SQL 内联 `AUTO_INCREMENT PRIMARY KEY` 写法不匹配 → 双形态兼容。

### W2-B · B-09 无环校验（本轮）
- **产出**：`GraphCycleDetector`——新增边校验（自环/重复边/成环）、**环路径回显 [to,…,from,to]**（3310 高亮路径）、全图体检 DFS。
- **证据**：`GraphCycleDetectorTest` 6/6 绿（菱形 DAG 不误报、回边闭合序、体检稳定回显）。
- **坑**：① record 组件 `ok` 与静态工厂 `ok()` **同名冲突**（编译错）→ 工厂改 `passed()`；② 原文手误 `List.of.copyOf`；③ 环序拼接方向（先 `[from]+path` 后改 `path+[to]` 以匹配回显语义）；④ 体检起点用 HashSet 序不稳 → 按边出现序 LinkedHashSet。

### W2-C · B-10 内容发布快照状态机 BR-06（本轮）
- **产出**：`ContentPublication`——工作稿 status 与 completeness 独立、**首读空四卡+missing=全部（绝不回草稿）**、部分发布（仅 approved 入快照）、`expectedRevision` 冲突 **3012**、显式 unpublish 清快照、quality=ai_draft 不赋可见权。
- **证据**：`ContentPublicationTest` **9/9 绿**（含"草稿正文不泄漏"断言、3012 冲突、AI 质量不赋权）。
- **联动**：app `ErrorCode` 新增 `REVISION_CONFLICT(3012)`（**待同步 04 §3 → Z-02 文档回写项**）。

> 本轮合计：`mvn test` **73/73 绿** + 迁移结构校验 23/23（证据同上）。

---

## 待续（W2 剩余）
- ~~B-08 知识点树+CSV 解析 · B-11 课时完成策略(BR-02 矩阵) · B-12 题库导入+judge_config 校验 · B-13 规则抽题（不足/放宽）~~ → **W2-D 已全部完成，见下**
- 其后各块 CRUD/HTTP 接线（编译级验证）→ 全绿翻 ✅ → 逐块提交推送。

### W2-D · B-08/B-11/B-12/B-13 收尾批（**W2 波次出口达成**）
- **B-08 `KnowledgeTreeParser`**：RFC4180 子集（引号/`""` 转义）、path 层级解析、逐行报错（行号）、同级重名/空段/难度域校验。证据 **7/7**。
- **B-11 `LessonCompletionPolicy`**：20 §2 五行矩阵逐项（未读/随堂未答/79.9 未过/80 过/read_only/幂等）、发布拦截（无客观题禁 practice_required）、首次 +10。证据 **7/7**。
- **B-12 `QuestionImportParser`**：CSV 逐行（答案键⊆选项键、节点≥1、行号报错不杀好行）+ `JudgeConfig` 规则（容差仅填空 [0,0.1]、比例计分仅填空）。证据 **8/8**。
- **B-13 `PaperRuleBuilder`**：确定性选取（难度↑→id↑）、不足→"按难度放宽至 Lx-Ly"或"覆盖门禁补内容"（20 §11）、族排除（竞赛不复用曝光族）、题型排除。证据 **7/7**。
- **本轮合计：`mvn test` 102/102 绿（13 测试类）**；累计模块 **✅14/75**，W2 波次 7/7 全部完成。
- **坑（全部测试自身问题，实现无误）**：① `ImportResult.ImportError` 类型引用错（应为平级 `ImportError`）；② 引号转义用例输入未整体加引号；③ parentPath 段数预期错（代数/方程=2 段）；④ 抽题同档排序预期错（应 #2→#7）。运行测试的价值正在于此。

---

## W3 · 行为域（进行中：B-14/B-15 ✅ → 累计 16/75）

### W3-A · B-14 作答状态机（BR-04 + BR-07）
- **产出**：`AttemptLifecycle`——四态推进（无解答题 SUBMITTED 直达 FINALIZED；unrated 阻塞自评完结、skipped 算已选）、成绩拆分（纯解答卷 rate=null+"本卷无客观成绩"）、**SettlementLedger 结算恰一次**、`deadlineAt=min(startedAt+duration, challengeEndAt)` 且**等于截止即过期**、草稿三型回执（requestId 重放/revision 冲突两份确认/到期拒绝）、finalized 改评不重放、开卷不计竞技。
- **证据**：`AttemptLifecycleTest` **16/16 绿**。
- **坑**：① 提示文案内嵌 ASCII 引号截断字符串（编译错 135 列）→ 去引号；② 测试文件漏 `import org.junit.jupiter.api.Test`（每个 @Test 报"找不到符号"）→ 补 import。

### W3-B · B-15 辅助矩阵（BR-03）
- **产出**：`AssistanceMatrix`——十种 mode→五组 (feedback, assistance) 默认、**服务端冻结+客户端篡改标记**、`can(capability, phase)` DURING 逐策略判定（restricted 全禁 / reference_only 仅公式表无链接 / open_book 公式推导✓题目深钻✗）、AFTER_SUBMIT 约束解除、开考告知四行投影、单开守卫(3007)、竞技题源硬约束（解答/未审/已公开同族拒绝）。
- **证据**：`AssistanceMatrixTest` **9/9 绿**。
- **本轮合计：`mvn test` 127/127 绿（15 测试类）。**

### W3-C · B-16/B-17/B-18 收尾批（**W3 波次出口达成**）
- **B-16 `BreakEvidence`（BR-05）**：四状态推导（客观错→observed；填动机/简答→自报不计；AI→suggested 确认转 self_reported）、证据去重键（answerId|source|stepId）、**链快照抗重排 + 禁跨版本同 seq 跳转**、AI 降级"无需等待"、补救 2-1-0 如实展示。证据 **7/7**。
- **B-17 `ComplianceRules`（U-06/U-07 + T61）**：2FA→7 天冷静（2008/2009）→撤销窗→到期匿名化（PII 清单+社区三态显示）、导出链接 7 天、**每日题仅 curated 排期**、公开每日族禁入竞技（BR-01 隔离）。证据 **6/6**。
- **B-18 `StatsAggregation`（02 §7/01 U-70）**：周汇总（**除零→null 不给 100%**）、环比 prev=0→不可用、画像 rate+小样本旗（仅 observed 喂入）、连签断链（今天缺→从昨天算）、热力 5 档、**周报规则模板回落（AI 无 Key）**。证据 **9/9**。
- **本轮合计：`mvn test` 149/149 绿（18 测试类）· ✅19/75 · W1/W2/W3 三波出口达成。**
- **坑**：断链 AI 降级文案断言写成 `不含"等待"`，与"无需等待"子串冲突（预判命中）→ 改正向断言。

---

## W4 · 路径与三内核（进行中：B-19/B-20/B-21 ✅ → 累计 22/75）

### W4-A · B-19/B-20/B-21 三块
- **B-19 `GraphProjection`**：parent 子树展开、**色档优先级（筹备>红锁>灰>分数档）**、必需边锁定（推荐边不锁、样本不足也算未掌握）、边版本化缓存键（`graph:adj:v{n}` 失效语义）。证据 **7/7**。
- **B-20 `AssessmentEngine`**（02 §5.6/A10）：档 2 起、**连对 3 升/错 2 降（窗口随切换清零）**、**终止优先于升降**、25 题帽、1-5 钳制、**续答状态回放（record 等价）**、维度 rate 零样本→null、报告双旗。证据 **8/8**。
- **B-21 `PlanSuggestions`**（BR-10/DR-10）：客观完成必须服务端证据（自报仅阅读型且打标）、absent 五态序列（skipped/rescheduled 不计不清）、**建议优先级缺席>证据规则**（<10 不足/<60 复测/≥80+三任务日 加速）、apply 版本冲突 **3011 带新旧差异**、dismiss 窗口静默、undo 双条件（版本未变且无新证据）、placement=pending 校准**永不自动覆盖**。证据 **6/6**。
- **本轮合计：`mvn test` 170/170 绿（21 测试类）。**
- **坑**：① `List.of(...)))` 少一个收括号（assertEquals 未闭合）→ 语法错定位修复；② 邻接表断言漏了推荐边 1→4（实现正确）→ 修断言。

### W4-B · B-22/B-23/B-24/B-25 收尾批（**W4 波次出口达成**）
- **B-22 `DeepDrillDomain`**：typed 路由 `/deepdive/{question|formula}/{id}`（防碰撞+旧路由重定向）、预测键统一解法/版本/stepId（19 R09）、客观双分支点评（对=强化动机 / 错="你会这么想…但…"+岔路）、**主观型恒自报不计 observed**。证据 **6/6**。
- **B-23 `FiveStepWorkspace`**：cellKey=answerId|step **逐题隔离**（同卷两题永不共用）、seq-step 序位校验、requestId 幂等/revision 冲突、**过程分只算前四步（VERIFY 不计分母）且恒不进客观成绩**、回代跳过=留痕非失败。证据 **6/6**。
- **B-24 `FormulaHall`**：alias 倒排（name+别名大小写归一）、**条件摘要=红条锚点≠完整条件**、missingPieces 七件套待补全、符号表（含义/定义域单位，无单位标"无量纲"）、proof=4 未证实徽标不可引定理、**小练三型有多少显示多少**、误用式分离。证据 **7/7**。
- **B-25 `NavigationIntent`（BR-09/DR-09）**：type/action 双白名单、**30min 随机 resumeToken 仅登记资源**、兑换绑定 24h 幂等/跨账号拒、导航完成 consumed 幂等、分享 URL 仅公共目标来源、**不可用文案保输入+搜索/推荐/复制出口**（不静默落首页）、解析不建记录、落地策略目标优先。证据 **7/7**。
- **本轮合计：`mvn test` 196/196 绿（25 测试类）· 后端 W1-W4 全出口（B-00~B-25=✅26）。一把过，无修复轮。**

---

## W5 · 后端收官波（B-26~B-30 全部 ✅ → **Track-A 后端 31/31 全清**）

### W5 五块批（🏁 后端全量完成）
- **B-26 `GamificationRules`**：打卡幂等 3501、补签 3502+卡守卫（只能补过去）、连签"贴今/昨"口径、成就三型解释器（未知型不解锁防误发）、每日任务须服务端事件、周榜积分降序+同分时间序。证据 **7/7**。
- **B-27 `CommunityModeration`**：**真 Aho-Corasick**（多模式重叠命中、分组归属、构建新实例热替换）、提交命中→HELD(3401 作者见"审核中"他人隐)、审核通过/驳回/举报下架/成立删除全路径、3403 点赞去重、采纳幂等。证据 **7/7**。
- **B-28 `AiGateway`**：3100 开关 / 3101（前 3 免费+余额守卫）/ 3102 上游；**SSE 帧构建-解析回环**（引号转义防破帧、[DONE] 终结）；默认苏格拉底可显式切直接；计划 JSON 校验→规则回落；AI 题未审不进卷。证据 **6/6**。
- **B-29 `NotifySubscribe`**：模板键四类归属（report_result 归审核优先）、深链、分类维度已读、邮箱正则、订阅/退订幂等+可回环。证据 **5/5**。
- **B-30 `ApiContract`**：路径方法唯一、前缀↔auth 一致、◎ 标注、**错误码须先入 ErrorCode**（与枚举自检）、JSON **确定性排序**（同 key 二级序）、模块计数看板。证据 **7/7**。
- **本轮合计：`mvn test` 228/228 绿（30 测试类）。**
- **坑（3+2 修复轮）**：① ApiContract 漏 `import common.ErrorCode`；② AC 遍历写成 `(Node) getClass()==…` 非法转换 → 改 while 走 fail 链；③ toJson 同 key 排序不稳定（**实现问题**）→ 二级排序保证输入无关；④ streak 测试集合漏"昨天"、⑤ 校验测试文案误含 ◎ —— 后两处为测试数据错。

---

## W6 · 前端轨开局（P-0 + W-00 ✅ → 累计 33/75）

### W6-A · P-0 双包 + W-00 web 脚手架
- **P-0 `@learnmath/shared` + `@learnmath/api-client`**：
  - shared：ApiResponse/Paginated 类型、错误码镜像（04 §3 文案+ApiError）、**SSE 分块解析器**（任意字节切开重组、CRLF、`:` 注释、多行 data、[DONE]）、locales 兜底。证据 **Vitest 10/10 + tsc ✓**；
  - api-client：注入式 Transport HTTP 客户端 —— 业务解包（code≠0→ApiError）、**401 单飞刷新**（并发 5 请求只刷 1 次、每人恰重试 1 次共 10 调用、**失败清在飞标记可再试**）、403/500 映射、query 构建；auth 模块薄封装（login/refresh/logout 路径对齐 04）。证据 **Vitest 7/7 + tsc ✓**。
- **W-00 `@learnmath/web`**：Vite 6.4.3（28180 strictPort）+ Pinia2 + vue-router4、WD1 顶导航骨架、**24 路由表**（typed 深钻 /deepdive/:type/:id 等，仅 /login 公开，全懒加载）、守卫（guard.ts 纯函数 + router 接线 + document.title）、**开放重定向防护**（//、\、http(s) 一律回首页）、tokens.css（05 §5 学习端黑体亮色）、W01-W18 十八占位视图（待屏级模块替换）。证据 **Vitest 7/7 + vue-tsc ✓**。
- **本轮合计：累计 252 测试全绿**（Java 228 + TS 24）+ 三包 typecheck ✓ + 迁移校验 23/23。
- **坑（5 处修复）**：① `packages/*` 为早间占位件（写入被守卫拦 → 读回合并完整版）；② modules/auth 相对路径 `./http.js` 应为 `../http.js`（**type-only import 逃过 vitest 但 tsc 抓住**）；③ 测试泛型缺 access 字段；④ store load 键覆盖告警 TS2783；⑤ `createWebHistory` 在 node 无 window → **memory history 兜底**（零依赖可测）。另：守卫接线首版写了 require 占位/坏 mount —— 整文件干净重写。

### W6-B · W-01 登录块
- **产出**：`loginPolicy.ts`（渠道主次与隐藏回退、手机归一/校验、**拦截优先级 协议2007→格式→频控1004**、协议版本过期、`loginSuccessRoute` 防开放重定向落地、**测评提示分型：落地非首页=banner 非阻塞 / 首页=modal S03**、续学提示语）+ `W01Login.vue` 实装（两步态/冷却倒计时/ApiError 文案）+ `services/client.ts` 单例（bare+带刷新双客户端）+ api-client `sendSmsCode` 扩展。
- **证据**：loginPolicy **Vitest 9/9**、web 全量 **16/16**、vue-tsc ✓、api-client tsc ✓ → **累计 261 测试**。
- **坑**：`assessmentPromptPlacement` 初版用"redirect 真值"判有目标，**被拦截的开放重定向会误判 banner** → 改为"有效落地是否首页"（测试先行抓出）。

### W6-C · W-02 首页块
- **产出**：`homeAggregation.ts`（任务进度四舍五入+空态护栏、打卡按钮态与**乐观 +1 幂等**、课程环除零、每日一练分解文案、**热力分档与后端 StatsAggregation 同口径**、路径推荐稳定排序=进行中优先→进度降序→截 N、测评 banner 非阻塞）+ `W02Home.vue` 实装（fixture 注入待 B30 契约接线）。
- **证据**：**Vitest 10/10**、web 全量 **26/26**、vue-tsc ✓ → **累计 271 测试**。
- **坑**：① `topPathRecs<PathRec>(...)` 给非泛型函数加类型参数 → TS2558（vue-tsc 抓出）；② 进度环 `conic-gradient` 里混进脏 token `var(--p) var(--brand)` → 修为单变量（typecheck 不查 CSS，人眼补刀）。

### W6-D · W-03 三屏块（路径中心+课程树+课时页）
- **产出**：`courseTree.ts`（折叠幂等、**学生端仅见 published（草稿整支隐藏）**、深度标注扁平化、锁标签 3311 兜底、章节摘要、六路径行、**BR-02 完成五态展示镜像**（句式与 20 §2 一致：阅读已记录…/不代表掌握/幂等）、奖励仅首次 +10、随堂练 optionMark/80% 达标口径）+ `W03Course.vue`（路径行/树形目录+锁态+当前章/前置卡）+ `W04Lesson.vue`（双栏+右栏随堂练两题交互+完成提示条）。
- **证据**：**Vitest 12/12**、web 全量 **38/38**、vue-tsc ✓ → **累计 283 测试**。
- **坑**：W04 `markDone` **先翻 alreadyCompleted 再读 reward → 恒得 0**（预检抓出，先取奖励再置位）。

### W6-E · W-04 作答+报告（Web 最重屏）
- **产出**：`attemptUi.ts`（答题卡样式优先级、**工具条光标处插入**、倒计时格式化与四档预警+**等于即过期**、快捷键映射（**输入态一律不劫持含 Ctrl+K**）、**草稿键 userId+attemptId 隔离**+脏数据安全解析+revision 三态（两份答案确认语义）、交卷文案未答列题号（>6 截断）、**报告客观-自评拆分**（纯解答卷"本卷无客观成绩"、自评五值标签/参考分/skipped 算已选/rated 阻塞出"继续自评"））+ `W08Paper.vue`（WD4 左题右卡、实时倒计时 tone、mathbar、keydown 全局监听、交卷确认弹层）+ `W09Report.vue`（拆分大卡+纯解答卷演示开关、错题断链行、自评三键+暂不评价、AI 归因）。
- **证据**：**Vitest 20/20**、web 全量 **58/58**、vue-tsc ✓ → **累计 303 测试**；**一把全绿零修复轮**。

### W6-F · W-05 深钻三区（产品灵魂屏 Web 形态）
- **产出**：`deepdiveUi.ts`（五玩 Tab query 合法化与索引、**步卡→右栏三层联动**（首步回退保非空）、**预测双分支渲染**（客观 idle/right/wrong + 主观恒 self「对照参考+不计入推理画像」句式同后端、选项标记）、**顺逆 id 对齐**（保 forward 序/对齐率/同色色序/未对齐灰）、**多解表**（通法标注/最快解=步数→技巧稳定排序/通法两级策略句）、typed 路由兜底）+ `W05Deepdive.vue`（WD3：顶五玩 Tab 联动 query、左链+右栏点选联动、双画布同色对齐率徽标、对比表、AI 入口带 stepSeq 上下文）。
- **证据**：**Vitest 13/13**、web 全量 **71/71**、vue-tsc ✓ → **累计 316 测试**。
- **坑（3 处，全测试侧/模板侧）**：① 遗留 `<textarea v-model>` 绑 boolean → TS2322；② 顺逆测试数据 B 漏 s1 且断言错位 pairs[0]；③ 修数据后断言串未同步（"分解"→"需分解"）。**实现零缺陷**。

### W6-G · W-06 图谱画布 + 节点四卡
- **产出**：`graphUi.ts`（**色档六态与后端 MasteryCalculator 同口径**（筹备>锁定>无数据>样本不足>79.99黄/80绿边界）、hex/文案/图例六项、**徽标筹备(紫)与锁定(红)区分**（BR-11）、**四卡缺失=中文名清单+待补全可达**、完整度%、名称过滤 trim 忽略大小写、**一跳邻域双向收集**）+ `W06Graph.vue`（画布节点+SVG 虚线边+检索过滤+图例+右侧抽屉：掌握环/四卡 Tab(缺卡叹号)/代表题/邻域 chips/锁态 3311 提示）+ `W07Node.vue`（详情页：四卡大图+完整度+代表题独立列+关系 chips+关联资产）。
- **证据**：**Vitest 10/10**、web 全量 **81/81**、vue-tsc ✓ → **累计 326 测试**；一把全绿。













