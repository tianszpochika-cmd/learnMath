server:
  port: 8080

spring:
  application:
    name: learn-app-api
  datasource:
    url: jdbc:mysql://localhost:3306/learnmath?useUnicode=true&characterSet=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: learnmath_root_2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
